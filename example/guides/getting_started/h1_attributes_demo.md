# Feature Demo {label="Implemented"}

This page demonstrates H1 attributes. The title above uses `{label="Implemented"}` — it appears as the page name in the sidebar and as a status badge, without being duplicated in the content.

You can also use multiple attributes: `{label="Beta" status="beta"}`.
