---
name: v0.1 (Beta)
order: 5
icon: flag-02
status: deprecated
---

## v0.1.0 — Beta

*Released: February 2026*

Initial development release (EchoDocs, before the Echox rename). Project init and first folder-based docs structure.

### Features

- Basic markdown rendering with Astro
- Folder-based navigation (tabs + sidebar groups)
- CLI with `dev` and `build` commands

### Migration to v1.0

Upgrade to the first stable release:

1. Update the package: `npm install -g echox@latest`
2. Add a `color` field to `config.json` (optional, defaults to `"blue"`)
3. Add `icon` frontmatter to pages if you want custom sidebar icons
4. Dark mode and full theming work automatically — no config needed
